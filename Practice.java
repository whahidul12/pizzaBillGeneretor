import java.util.Scanner;
import java.lang.Math;

public class Practice {

  public static void main(String[] args) {

    int a, b, n;
    Scanner scanner = new Scanner(System.in);
    int q = scanner.nextInt();
    for (int i = 0; i < q; i++) {
      a = scanner.nextInt();
      b = scanner.nextInt();
      n = scanner.nextInt();
    }
    scanner.close();
    for (int i = 0; i < n; i++) {
      for(int j = 0; j <= i; j++){
        
      }
      double r = (a + (Math.pow(2, i)) * (OP) * b);

    }
  }
}